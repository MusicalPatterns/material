// tslint:disable no-magic-numbers

import { negative, Ordinal, to } from '@musical-patterns/utilities'

const TEMPORARY_UNDEFINED_SEGNO_INDEX: Ordinal = to.Ordinal(negative(2))

export {
    TEMPORARY_UNDEFINED_SEGNO_INDEX,
}
