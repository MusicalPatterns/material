// tslint:disable no-magic-numbers

import { Index, negative, to } from '@musical-patterns/utilities'

const TEMPORARY_UNDEFINED_SEGNO_INDEX: Index = to.Index(negative(2))

export {
    TEMPORARY_UNDEFINED_SEGNO_INDEX,
}
